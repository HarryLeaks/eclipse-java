/* 
 * 	Client(Socket) -> Network <- Server(Server Socket)
 * 
 * client -> Request -> Server -> accept the connection -> After the Socket is created
 * 
 * Socket able us to create a peer to peer communication with a server in the middle that accepts the communication
 */ 

